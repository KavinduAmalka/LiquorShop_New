# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs
build/
dist/
.next/

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp

# KEEP THESE IMPORTANT FILES FOR PROJECT SECURITY AND TESTING:
# !backend/logs/ - Keep all log files for security monitoring
# !scripts/ - Keep all PowerShell scripts for security testing
# !test-security-logging.js - Keep security testing suite
# !simple-a09-check.ps1 - Keep A09 verification tool
# !TESTING_AND_LOGGING_FILES.md - Keep documentation

# Only ignore very large log files (>100MB) to prevent repo bloat
backend/logs/*.log
# But keep recent logs (override above)
!backend/logs/*-$(Get-Date -Format 'yyyy-MM-dd').log

# Keep audit files for Winston daily rotation
!backend/logs/*.json
